# Generated dataset batches

The pipeline writes one timestamped JSONL file here after each successful
quality-gated run. Each line uses the same flattened row format sent to the
configured Hugging Face dataset repository.

GitHub Actions commits new batch files automatically.
